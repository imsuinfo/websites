name        = CSV
description = "A minimalistic theme designed for CSV content. This does not enforce that the content is in the proper format."
version     = "7.x-1.0"
core        = 7.x
engine      = phptemplate
screenshot  = screenshot.png

regions[content] = Content

; features[] = logo
; features[] = name
; features[] = slogan
; features[] = node_user_picture
; features[] = comment_user_picture
; features[] = search
; features[] = favicon
; features[] = main_menu
; features[] = secondary_menu
