<?php
// $Id$

/**
 *  @file
 *  Integrating Nivo-slider with Views jQFX.
 */

/**
 * Implements hook_theme().
 */
function msu_slider_theme($existing, $type, $theme, $path) {
  return array(
    'msu_slider' => array(
      'variables' => array('view' => NULL, 'options' => array(), 'rows' => array(), 'title' => ''),
      'template' => 'msu_slider',
      'file' => 'msu_slider.theme.inc',
      'path' => $path . '/theme',
    ),
  );
}
